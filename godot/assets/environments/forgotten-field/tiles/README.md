# Forgotten Field tiles

Drop normalized 96 × 48 top-diamond PNGs here using the exact names in `godot/data/asset_manifest.json`. Side depth may extend the canvas to 64 px high. Transparent backgrounds are required.
