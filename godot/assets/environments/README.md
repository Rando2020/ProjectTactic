# Environment assets

Each environment theme owns a folder containing its replaceable tiles and props. Runtime paths are declared in `godot/data/asset_manifest.json`; gameplay code should reference theme and asset IDs, not files directly.
